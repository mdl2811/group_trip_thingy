
<h1>Roads? Where we're going we don't need roads.</h1>
<br />

<h3><%= current_user.email %> Your password is: <%= current_user.encrypted_password %></h3>
<h3>Your Trips:</h3>

<% if @trips.any? %>
	<ul>
		<% @trips.each do |trip| %>
		<li>
			<h4><%= trip.info %></h4>
			<%= link_to 'Details of Trip', trip_path(trip) %>
			<span>|</span>
			<%= link_to 'Edit', edit_trip_path(trip) %>
			<span>|</span>
			<%= link_to 'Delete', trip_path(trip), data: {confirm: 'Do you really want to delete?'}, method: :delete %>
		</li>
		<% end %>
	</ul>
<% end %>

<br />
<br />

<%= link_to 'Plan an Adventure!', new_trip_path, class: 'btn btn-primary' %>
<br />
<br />

<div class="container center">
	<h1>Search around the map for where you'd like to go..</h1>
	<iframe src="https://www.google.com/maps/embed?pb=!1m14!1m8!1m3!1d12580.959918787094!2d23.7267166!3d37.9715285!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x0%3A0x1b3fa079b878a218!2sParthenon!5e0!3m2!1sen!2sus!4v1496687397096" width="800" height="600" frameborder="0" style="border:0" allowfullscreen></iframe>
</div>
